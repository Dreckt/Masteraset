export default function SetsPage() {
  return (
    <main style={{ padding: 32 }}>
      <h1>Sets</h1>
      <p>This confirms /sets is wired up in the live router.</p>
      <p>Next: we’ll move your real UI from <code>src/app/sets</code> into this route.</p>
    </main>
  );
}
